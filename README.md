# To-Do List App (React + TypeScript)

یک اپلیکیشن مدیریت وظایف (Task Manager) با رابط کاربری فارسی و راست‌به‌چپ که با **React**، **TypeScript**، **MUI** و **Zustand** پیاده‌سازی شده است.

این پروژه شامل ایجاد، ویرایش، حذف، جستجو، فیلتر و تکمیل وظایف بوده و اطلاعات در `localStorage` ذخیره می‌شوند تا بعد از رفرش صفحه از بین نروند.

---

## ✨ امکانات پروژه

- افزودن وظیفه جدید با عنوان و توضیحات
- ویرایش وظیفه‌های ثبت‌شده
- حذف وظیفه
- علامت‌گذاری وظیفه به‌عنوان انجام‌شده / انجام‌نشده
- حذف گروهی وظایف انجام‌شده
- جستجو روی عنوان و توضیحاتdd
- فیلتر وظایف بر اساس وضعیت:
  - همه
  - در حال انجام
  - انجام شده
- نمایش آمار کلی (کل، انجام‌شده، باقی‌مانده)
- نمایش پیام موفقیت عملیات با `Snackbar`
- پشتیبانی از RTL و فونت فارسی

---

## 🧱 تکنولوژی‌ها

- **React 19**
- **TypeScript**
- **Material UI (MUI)**
- **Zustand** برای مدیریت state
- **LocalStorage** برای ماندگاری داده‌ها

---

## 🗂️ ساختار کلی پروژه

```bash
src/
  components/
    layout/
      AppHeader.tsx
    tasks/
      TaskToolbar.tsx
      TaskList.tsx
      TaskCard.tsx
      TaskFormModal.tsx
  store/
    taskStore.ts
  types/
    task.ts
  App.tsx
```

---

## ⚙️ اجرای پروژه

ابتدا dependencies را نصب کنید:

```bash
npm install
```

اجرای پروژه در محیط توسعه:

```bash
npm start
```

نسخه production:

```bash
npm run build
```

---

## 📌 نکات پیاده‌سازی

- مدیریت داده‌ها در `src/store/taskStore.ts` با Zustand انجام شده است.
- برای جلوگیری از خرابی داده‌های ذخیره‌شده، هنگام بارگذاری `localStorage` داده‌ها normalize می‌شوند.
- فرم ثبت/ویرایش دارای اعتبارسنجی اولیه (الزامی بودن عنوان و حداقل طول ۳ کاراکتر) است.
- تم و تنظیمات ظاهری (رنگ، فونت، RTL) در `App.tsx` تعریف شده‌اند.

---

## 📜 اسکریپت‌ها

- `npm start` → اجرای پروژه در حالت توسعه
- `npm run build` → بیلد production
- `npm test` → اجرای تست‌ها

---

## 👨‍💻 توسعه‌دهنده

این README برای ارائه بهتر پروژه در GitHub به‌روزرسانی شده است.
